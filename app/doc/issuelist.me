### 1. 등록일, 수정일 등 일시를 입력해야하는 컬럼의 경우에 timestamp(), datetime() ... 등 뭘 넣어야 되나?
#### 1-1. 방안 a)  데이터타입 : datetime(), default : now() = current timestamp()
#### 1-2. 방안 a) 문제점 : mariadb 버전이 다른경우, 지원안되는 함수가 있음 : 로컬 mariadb 버전과 원격 db버전이 달라서 원격에서 datetime()이 적용되지 않음
#### 1-3. 

### 2. 1:1 문의 게시판의 경우, ask테이블 하나에서 컬럼으로 질문과 답변을 모두 구분하고 있는데, insert를 어떻게 해야하나?
#### 2-1. 해결방안) record가 처음 db에 기록될 때 insert는 1번만 되는 것, record가 만들어졌으니 그 이후, 데이터 입력은 모두 update로 되는 것  (record를 바라본다.)
ask컬럼(문의)의 경우, insert/update문 작성해야 되고, answer컬럼(답변)의 경우, update문 작성해야함 

#### 2-2. 비슷한 예) 회원가입시 회원테이블에 레코드가 1개 생성(insert): 가입일자 칼럼/ 그 이후 로그인 되는 경우는 모두 update : 최종접속일시 칼럼