<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.pompeu.admin.member.dao.MemberDao">

  <resultMap type="memberType" id="memberTypeMap">
    <id column="member_type_no" property="memberTypeNo"/>
    <result column="member_type" property="memberType"/>
  </resultMap>


  <resultMap type="member" id="memberMap">
    <id column="member_no" property="no"/>
    <result column="member_type_no" property="memberTypeNo"/>
    <result column="sns_no" property="snsNo"/>
    <result column="join_date" property="joinDate"/>
    <result column="name" property="name"/>
    <result column="email" property="email"/>
    <result column="phone" property="phone"/>
    <result column="nickname" property="nickName"/>
    <result column="use_check;" property="useCheck"/>    
    <result column="login_date" property="loginDate"/>
    <result column="modify_date" property="modifyDate"/>
    <result column="admin_check" property="adminCheck"/>
    <result column="password" property="password"/>
    <result column="birth" property="birth"/>
    <result column="gender" property="gender"/>
      <collection property="memberType" resultMap="memberTypeMap"/>
  </resultMap>


  <resultMap type="users" id="usersMap">
    <id column="users_no" property="no"/>
    <result column="image" property="image"/>
    <result column="ex_type_no" property="exTypeNo"/>
  </resultMap>

  <resultMap type="myclassList" id="myclassListMap">
    <id column="myclass_list_no" property="no"/>
    <result column="users_no" property="usersNo"/>
    <result column="class_time_no" property="classTimeNo"/>
    <result column="register_member" property="registerMember"/>
    <result column="pay_status" property="payStatus"/>
    <result column="content" property="content"/>
    <result column="registerDate" property="registerDate"/>
    <result column="rate" property="rate"/>
  </resultMap>

  <resultMap type="classTime" id="classTimeMap">
    <id column="class_time_no" property="no"/>
    <result column="class_no" property="classNo"/>
    <result column="start_time" property="startTime"/>
    <result column="end_time" property="endTime"/>
    <result column="max_member" property="maxMember"/>
  </resultMap>

  <resultMap type="class" id="classMap">
    <id column="class_no" property="no"/>
    <result column="location_no" property="locationNo"/>
    <result column="creator_no" property="creatorNo"/>
    <result column="ex_no" property="exNo"/>
    <result column="name" property="name"/>
    <result column="start_date" property="startDate"/>
    <result column="end_date" property="endDate"/>
    <result column="class_price" property="classPrice"/>
    <result column="class_info" property="classInfo"/>
    <result column="register_date" property="registerDate"/>
    <result column="status" property="status"/>
    <result column="admin_message" property="adminMessage"/>
    <result column="total_rate" property="totalRate"/>
    <result column="in_out_ex" property="inOutEx"/>
  </resultMap> 

  <select id="countAll" resultType="int">
    select count(*) from member
  </select>
  
  <select id="findAll" resultMap="memberMap">
    select 
      member_no,
      name,
      email,
      phone,
      nickname,
      login_date,
      join_date
    from 
      member 
    order by 
      member_no asc
  </select>
  
  <insert id="insert" parameterType="member">
    insert into member(member_type_no, name,email,phone,nickname,use_check,join_date,modify_date,admin_check,password,birth,gender,sns_no) 
    values(#{memberTypeNo},#{name},#{email},#{phone},#{nickName},#{useCheck},#{joinDate},#{modifyDate},#{adminCheck},#{password},#{birth},#{gender},#{snsNo})
  </insert>
  
  <select id="findByNo" resultMap="memberMap" parameterType="int">
    select  
          mt.member_type                                        as member_type,
          m.name                                                as name,
          m.nickname                                            as nickname,
          m.email                                               as email,
          m.phone                                               as phone,
               m.join_date                                           as join_date,
               m.login_date                                          as login_date
    from 
          member  m
          join member_type mt 
                on m.member_type_no = mt.member_type_no 
    where 
      m.member_no=#{no}
  </select>

<<<<<<< HEAD
=======
  <!--select id="findByNoClass" resultMap="HashMap" parameterType="int">
    select  
          c.name                                                as name,
          c.start_date                                          as start_date,
          c.end_date                                            as end_date,
          c.status                                              as status
    from 
          member as m
    join users as u 
          on m.member_no = u.users_no
    join myclass_list as ml 
          on u.users_no = ml.users_no
      join class_time as ct 
          on ml.class_time_no = ct.class_time_no 
      join class as c
          on ct.class_no = c.class_no
    where 
      m.member_no=#{no}
  </select--> 

>>>>>>> branch 'main' of https://github.com/linarano/pompeu.git
  <update id="update" parameterType="member">
    update member set 
        name=#{name},
        email=#{email},
        phone=#{phone},
        nickname=#{nickName},
        use_check=#{useCheck},
        join_date=#{joinDate},
        modify_date=#{modifyDate},
        admin_check=#{adminCheck},
        password=#{password},
        birth=#{birth},
        gender=#{gender},
        sns_no=#{snsNo}
    where 
      member_no=#{no}
  </update>

  <delete id="delete" parameterType="int">
    delete from member 
    where member_no=#{no}
  </delete>
  
  <select id="findCount" resultType="hashmap">
    select count(*)                                                    as total
            ,  count(case when member_type_no = 1 then 1 else null end )        as user
            ,  count(case when member_type_no = 2 then 1 else null end )        as creator
          from member
         where member_type_no in (1, 2)
  </select>
  
<select id="srchMember" parameterType="member" resultMap="memberMap">
    select 
          m.member_no                                           as member_no,  
          mt.member_type                                        as member_type,
          m.name                                                as name,
          m.email                                               as email,
          m.phone                                               as phone
    from 
          member  m
          join member_type mt 
                on m.member_type_no = mt.member_type_no 
    where 1=1
        
    <if test="memberTypeNo != ''">
        and m.member_type_no = #{memberTypeNo}
    </if>
    <if test="name != ''">
        and m.name = #{name}
    </if>
    <if test="phone != ''">
        and m.phone = #{phone}
    </if>
    <if test="email != ''">
        and m.email = #{email}
    </if> 
       order by 
          m.member_no asc
  </select>

  <select id="findByNoClass" resultType="HashMap" parameterType="int">
    select  
          c.name                                                as name,
          c.start_date                                          as startDate,
          c.end_date                                            as endDate,
          c.status                                              as status
    from 
          member as m
    join users as u 
          on m.member_no = u.users_no
    join myclass_list as ml 
          on u.users_no = ml.users_no
		join class_time as ct 
          on ml.class_time_no = ct.class_time_no 
		join class as c
          on ct.class_no = c.class_no
    where 
      m.member_no=#{no}
  </select>


</mapper>